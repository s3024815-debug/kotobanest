<x-app-layout><x-slot name="header"><h2 class="font-bold text-2xl text-gray-800">Favorites</h2></x-slot><div class="py-10 bg-slate-50 min-h-screen"><div class="max-w-5xl mx-auto sm:px-6 lg:px-8"><div class="bg-white rounded-3xl shadow p-8">@forelse($favorites as $f)<div class="border-b py-4 flex justify-between"><div><b>{{ $f->title }}</b><p>{{ $f->type }}</p></div><form method="POST" action="{{ route('favorites.destroy',$f) }}">@csrf @method('DELETE')<button class="text-red-600 font-bold">Remove</button></form></div>@empty<p>No favorites yet.</p>@endforelse</div></div></div></x-app-layout>