<x-app-layout><x-slot name="header"><h2 class="font-bold text-2xl text-gray-800">Quiz Result</h2></x-slot><div class="py-10 bg-slate-50 min-h-screen"><div class="max-w-4xl mx-auto sm:px-6 lg:px-8"><div class="bg-white rounded-3xl shadow p-8 mb-6"><h3 class="text-4xl font-black text-blue-600">{{ $score }} / {{ $questions->count() }}</h3><p class="font-bold text-pink-600">XP Earned: {{ $xp }}</p></div><div class="space-y-4">@foreach($review as $r)<div class="bg-white rounded-2xl shadow p-5"><p class="font-bold">{{ $r['question'] }}</p><p>Your answer: {{ $r['answer'] ?: '-' }}</p><p>Correct: {{ $r['correct'] }}</p><p class="{{ $r['ok'] ? 'text-green-600' : 'text-red-600' }} font-bold">{{ $r['ok'] ? 'Correct' : 'Wrong' }}</p>@if($r['explanation'])<p class="text-gray-600">{{ $r['explanation'] }}</p>@endif</div>@endforeach</div></div></div></x-app-layout>