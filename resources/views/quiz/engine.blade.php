<x-app-layout><x-slot name="header"><h2 class="font-bold text-2xl text-gray-800">Real Quiz Engine</h2></x-slot><div class="py-10 bg-slate-50 min-h-screen"><div class="max-w-4xl mx-auto sm:px-6 lg:px-8"><form method="POST" action="{{ route('quiz.submit') }}" class="space-y-6">@csrf @forelse($questions as $q)<div class="bg-white rounded-3xl shadow p-6"><h3 class="font-black text-xl mb-4">{{ $loop->iteration }}. {{ $q->question }}</h3><input type="hidden" name="questions[]" value="{{ $q->id }}">@foreach(['A'=>$q->choice_a,'B'=>$q->choice_b,'C'=>$q->choice_c,'D'=>$q->choice_d] as $key=>$choice)@if($choice)<label class="block p-3 rounded-xl bg-slate-50 mb-2"><input type="radio" name="answers[{{ $q->id }}]" value="{{ $key }}"> {{ $key }}. {{ $choice }}</label>@endif @endforeach</div>@empty<div class="bg-white p-8 rounded-3xl shadow">No quiz questions yet. Add from Admin Quiz.</div>@endforelse @if($questions->count())<button class="px-6 py-3 bg-blue-600 text-white rounded-full font-bold">Submit Quiz</button>@endif</form></div></div></x-app-layout>