@extends('layouts.student') @section('title','My Account')
@section('content')
<div class="card"><div style="display:flex;justify-content:space-between;gap:20px;flex-wrap:wrap"><div><div style="width:90px;height:90px;border-radius:50%;background:#dbeafe;display:grid;place-items:center;font-size:36px;font-weight:900">{{ strtoupper(substr($user->name,0,1)) }}</div><h1>{{ $user->name }}</h1><div class="muted">{{ '@'.($user->username ?: 'user'.$user->id) }}</div></div><a class="btn" href="{{ route('account.edit') }}">Edit profile</a></div><hr style="border:0;border-top:1px solid #edf0f5;margin:22px 0"><div class="grid"><div><div class="muted">Email</div><strong>{{ $user->email }}</strong></div><div><div class="muted">Country</div><strong>{{ $user->country ?: 'Not set' }}</strong></div><div><div class="muted">Native language</div><strong>{{ $user->native_language ?: 'Not set' }}</strong></div><div><div class="muted">JLPT</div><strong>{{ $user->current_jlpt }}</strong></div></div>@if($user->bio)<p style="margin-top:24px">{{ $user->bio }}</p>@endif</div>
@endsection
