<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Vocabulary - KotobaNest</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="kb-body">
<header class="kb-nav"><a href="/" class="kb-logo"><span>あ</span>KotobaNest</a><nav class="kb-links"><a href="{{ route('home') }}">Home</a><a href="{{ route('lessons.index') }}">Lessons</a><a href="{{ route('vocabulary.index') }}">Vocabulary</a><a href="{{ route('quiz') }}">Quiz</a>@auth<a class="kb-login" href="{{ route('dashboard') }}">Dashboard</a>@else<a class="kb-login" href="{{ route('login') }}">Login</a>@endauth</nav></header>
<main class="kb-section"><div class="kb-title"><p class="kb-badge">Vocabulary System</p><h2>Learn Japanese words with furigana and examples</h2></div>
<form method="GET" class="kb-filter"><input name="q" value="{{ request('q') }}" placeholder="Search word, reading, meaning..."><select name="level"><option value="">All Levels</option>@foreach(['N5','N4','N3','N2','N1','Daily Life'] as $level)<option value="{{ $level }}" @selected(request('level')===$level)>{{ $level }}</option>@endforeach</select><select name="category"><option value="">All Categories</option>@foreach(['Daily Life','School','Work','Travel','Food','JLPT'] as $category)<option value="{{ $category }}" @selected(request('category')===$category)>{{ $category }}</option>@endforeach</select><button class="kb-btn kb-primary">Search</button></form>
<div class="kb-vocab-grid">@forelse($vocabularies as $v)<article class="kb-vocab-card"><div class="kb-vocab-top"><span class="kb-level">{{ $v->level }}</span><span class="kb-category">{{ $v->category }}</span></div><h3>{{ $v->word }}</h3>@if($v->furigana)<p class="kb-furigana">{{ $v->furigana }}</p>@endif<div class="kb-meaning"><b>English:</b> {{ $v->meaning_en }}</div>@if($v->meaning_bn)<div class="kb-meaning"><b>বাংলা:</b> {{ $v->meaning_bn }}</div>@endif @if($v->example)<div class="kb-example-box">{{ $v->example }}</div>@endif</article>@empty<div class="kb-card"><h3>No vocabulary found</h3><p>Add vocabulary from Admin Vocabulary page.</p></div>@endforelse</div><div class="kb-pagination">{{ $vocabularies->links() }}</div></main><footer class="kb-footer">© 2026 KotobaNest.</footer></body></html>
